package config

const (
	DUPLICATED_DATA      = "error, data duplicate in database"
	DATABASE_ERROR       = "error, database could not process"
	ENCRYPT_ERROR        = "error, cant encrypt the password"
	REJECTED_DATA        = "error, rejected from database"
	NO_DATA              = "no data in database"
	PARSE_DATA           = "error, cant parsing data"
	USERNAME_EMAIL_EMPTY = "error, username or email is empty"
)
